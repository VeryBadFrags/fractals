{
  "name": "vite-project",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "format": "prettier --write .",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "prettier": "2.8.3",
    "typescript": "^4.9.4",
    "vite": "^4.0.0"
  },
  "dependencies": {
    "@types/bootstrap": "^5.2.6",
    "bootstrap": "^5.2.3"
  }
}
